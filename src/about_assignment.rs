#[test]
fn we_can_declare_a_variable_without_setting_it() {
    let x;
    x = 3;
    assert_eq!(__, x);
}
#[test]
fn we_can_modify_mutable_values() {
    let mut x = 10;
    assert_eq!(__, x);
    x += 10;
    assert_eq!(__, x);
}


